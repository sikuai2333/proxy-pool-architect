# Phase 6 Prompt：Dashboard

```text
Implement Phase 6: basic dashboard.

Scope:
1. Add a minimal web dashboard or static admin page.
2. Show raw / checked / elite / dead counts.
3. Show provider source distribution.
4. Show average latency and success rate.
5. Add a delete action for a proxy.
6. Keep UI simple and avoid blocking backend MVP.

If using a frontend framework, justify the dependency. Otherwise implement a lightweight server-rendered or static page first.

Run:
- pytest
- ruff check .
- mypy app
```
